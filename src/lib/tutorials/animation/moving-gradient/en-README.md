# Moving Gradient

Animate a gradient by shifting vUv.x over time.

## Learning Objectives
- Use fract() for looping motion
- Animate UV-based factor

## Exercise
- Compute t = fract(vUv.x + u_time * speed)
- Use t in mix()

## Hints
- Try speed=0.2
