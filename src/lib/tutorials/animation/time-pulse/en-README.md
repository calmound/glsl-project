# Time Pulse

Create a pulsing brightness effect using sin(u_time).

## Learning Objectives
- Use u_time uniform
- Map sin() output to 0-1

## Exercise
- Compute pulse = sin(u_time) * 0.5 + 0.5
- Multiply base color by pulse

## Hints
- pulse in [0,1]
