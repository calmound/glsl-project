# 对角线渐变

你已经知道：`vUv.x` 可以得到从左到右的渐变，`vUv.y` 可以得到从下到上的渐变。本练习将它们组合起来，生成 **对角线方向** 的渐变效果。

## 练习要求

- 用 UV 构造渐变因子 `t`：`(vUv.x + vUv.y) * 0.5`
- 使用 `clamp` 将 `t` 限制在 `[0.0, 1.0]`
- 使用 `mix(colorA, colorB, t)` 输出最终颜色

