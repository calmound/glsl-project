# Polar Gradient (Angle)

Convert UV to polar angle and map it to a 0-1 gradient.

## Learning Objectives
- Use atan(y, x) to get angle
- Normalize angle into 0-1

## Exercise
- Compute t from atan(p.y, p.x)
- Use t to blend two colors

## Hints
- t = (a + PI) / (2*PI)
