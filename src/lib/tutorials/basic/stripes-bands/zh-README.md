# 条纹与带状图案

本练习将使用以下两个函数构造重复条纹：

- `fract(x)`：保留小数部分，使数值按 1.0 周期重复
- `step(edge, x)`：把连续值转换为 0/1 的硬边遮罩

## 练习要求

1. 将 `vUv.x` 乘以条纹数量
2. 用 `fract` 把结果限制在 `[0, 1)` 范围
3. 用 `step` 将每个条纹分成两半（0 与 1）
4. 用 `mix` 根据遮罩在两种颜色间切换

