# Remap Function

Implement a remap() helper to convert values between ranges.

## Learning Objectives
- Write a simple helper function
- Use it to colorize a gradient

## Exercise
- Use remap(vUv.x,0,1,-1,1) then abs()

## Hints
- t should be in 0-1 after abs
