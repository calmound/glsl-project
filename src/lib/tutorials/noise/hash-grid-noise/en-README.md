# Hash Grid Noise

Generate a random value per grid cell using a small hash function.

## Learning Objectives
- Build a simple hash function
- Use floor() to create grid cells

## Exercise
- Compute n = hash21(floor(vUv*grid))
- Output vec3(n)

## Hints
- Try grid=64
