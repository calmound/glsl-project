# Polka Dots

Build a dot pattern on a grid and draw circles in each cell.

## Learning Objectives
- Use fract() grid cells
- Draw circles with smoothstep

## Exercise
- Compute gv = fract(vUv*count)-0.5
- Build dot mask with smoothstep

## Hints
- Try radius ~0.18 in cell space
